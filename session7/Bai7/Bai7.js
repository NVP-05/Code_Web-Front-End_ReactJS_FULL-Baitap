"use strict";
function studens(id, name, lop) {
    let objs = {
        id: id,
        name: name,
        lop: lop,
    };
    console.log(objs);
}
studens(1, "Phúc", "Sáng");
studens(2, "Minh", "Chiều");
