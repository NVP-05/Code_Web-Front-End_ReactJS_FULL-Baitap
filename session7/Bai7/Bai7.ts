function studens(id:number, name:string, lop:string){
    let objs = {
        id: id,
        name: name,
        lop:lop,
    }
    console.log(objs);
}

studens(1, "Phúc", "Sáng");
studens(2, "Minh", "Chiều");
